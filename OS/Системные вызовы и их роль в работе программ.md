#### **1. Что такое системный вызов?**  
- **Определение**:  
  - Это обращение программы к ядру ОС для выполнения привилегированной операции (например, работа с файлами, памятью, устройствами).  
  - Примеры: `open()`, `write()`, `fork()` в Linux.  
- **Зачем нужны**:  
  - Программы работают в **пользовательском режиме** (user mode) и не имеют прямого доступа к железу.  
  - Для безопасности: сбой в программе не крашит всю систему.  
#### **2. Режимы работы процессора**  
| **Режим**       | **Доступ к ресурсам** | **Пример**                     |  
|------------------|-----------------------|--------------------------------|  
| **Пользовательский** | Ограниченный          | Приложения (браузер, текстовый редактор). |  
| **Ядра (Kernel)** | Полный                | Драйверы, системные вызовы.    |  
##### - **Переключение контекста**:  
  - При системном вызове процессор переходит из user mode в kernel mode (и обратно).  
  - Это требует времени — поэтому частые вызовы снижают производительность.  
#### **3. Как реализуются системные вызовы?**  
- **Исторически**:  
  - Использовались **программные прерывания** (например, `int 0x80` в Linux на x86).  
- **Современные CPU**:  
  - Специальные инструкции (например, `syscall`/`sysenter` в x86, `svc` в ARM).  
- **Пример для Windows**:  
  - `NTDLL.dll` содержит функции типа `NtCreateFile`, которые вызывают ядро через `syscall`.  
#### **4. Пример: Простая программа на Си**  
```c
#include <stdio.h>
int main() {
    printf("Всем привет!\n");
    return 0;
}
```  
**Есть ли здесь системные вызовы?**  
- **Да!** Даже такая простая программа их использует:  
  1. **Вывод текста**: `printf` → вызывает `write()` (системный вызов для записи в консоль).  
  2. **Завершение программы**: `return 0` → может вызывать `exit()` (освобождение ресурсов).  
- **Детали**:  
  - В **монолитных ядрах** (Linux, Windows) даже `printf` приводит к вызову ядра.  
  - В **микроядерных ОС** (например, QNX) вывод может обрабатываться сервисом в user mode.  
#### **5. Стандартизация системных вызовов**  
- **POSIX**:  
  - Попытка унифицировать API для Unix-подобных систем (Linux, macOS).  
  - Примеры функций: `open()`, `read()`, `fork()`.  
- **WinAPI**:  
  - Стандарт де-факто для Windows (например, `CreateFileW`).  
  - Не совместим с POSIX, что усложняет перенос программ.  
#### **6. Интересные факты**  
- **Количество вызовов**:  
  - Linux: ~300 системных вызовов.  
  - Windows: ~2000 (из-за сложности WinAPI).  
  - Plan 9: всего 50 (!) — минимализм и простота.  
- **Быстрые вызовы**:  
  - Инструкции `syscall` работают в 2-3 раза быстрее, чем старые прерывания.  
#### **7. Почему это важно?**  
- **Безопасность**:  
  - Программы не могут напрямую трогать железо — только через ядро.  
- **Переносимость**:  
  - POSIX-совместимый код легче портировать между Linux/macOS.  
- **Производительность**:  
  - Частые вызовы (`write()` в цикле) могут замедлить программу.  

---  
### **Практика**  
1. **Просмотр системных вызовов**:  
   - В Linux: `strace ./program` — покажет все вызовы программы.  
   - В Windows: `API Monitor` или `Process Explorer`.  
2. **Сравнение POSIX и WinAPI**:  
   - `open()` (POSIX) vs `CreateFile()` (Windows).  

---  
**Вывод**: Даже "простая" программа на Си активно взаимодействует с ядром через системные вызовы. Понимание этого механизма — ключ к системному программированию! 🚀

[[Операционныe системы]]