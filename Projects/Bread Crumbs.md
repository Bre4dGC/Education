интерпретируемый язык программирования написанный на C.
расширение: .bc

### Variable declaration:
```C
<тип данных> : <название>;              #Обьявление
<название> = <значение>;                #Присваивание
<тип данных> : <название> = <значение>; #Инициализация

#Пример:
int : age = 15;
string : name = "bread";
```
### Типы данных:
```C
#есть signed и unsigned
signed, unsigned

#основные типы данных
char = '#', -128, 127;
string ="Hello, World!"
short = -32768, 32767;
int = -2147483648, 2147483647;
long = 
decimal =
float = 
double =

#так же добавил типы int с количеством занимаемых ими байтами памяти
int_8  == char;
int_16 == short;
int_32 == int;
int_64 == long;

#для сокращения можно писать 'u_' в начале типов данных, так они будут unsigned
u_char = '#', 0, 256;
u_short = 0, 65535;
u_int = 0, 4294967296;
u_long =
u_decimal
u_float =
u_double =
```
### Условные выражения:
```C
#так как я нетакуся, выражение пишется не в круглых, а в угловых скобках
if <<выражение>> {
        <какой то код для if>
} else if <<expression>> {
        <какой то код для else if>
} else {
        <какой то код для else>
}

#Пример:
If <52 == "санкт-петербург"> {
       print("задалбала эта песня");
} else if <user == valentin> {
        print("валик");
} else {
        print("все окей");
}

#вместо
if <<выражение && выражение2 || выражение3>>
#нужно писать
if <<выражение>> && <<выражение2>> || <<выражение3>>

#Пример
if <1 > 2> && <2 == 3> || <4 < 10>

#как по мне так читабельно и локанично 
```

### Циклы
```C
while <<выражение>> {
        <какой то код>
}

for <init; expression;
```