интерпретируемый язык программирования написанный на C.
расширение: .bc

---
## **1. Dual Execution Mode (undo/rollback)**

### Суть:
Ты превращаешь любой код в "транзакционный", как база данных. Интерпретатор пишет журнал (`oplog`) и по запросу может:
- откатить переменные,
- сбросить память, 
- вернуться к точке до вызова функции.
### Плюсы:
- **Отладка без боли**: пошагово тестируешь код, не перезапуская.
- **Безопасные сайд-эффекты**: читаешь/пишешь файлы, потом `undo`.
- **Имплицитный time-travel debugger**.
### Как реализовать:
- Ввести концепт "снапшота" (начало блока, функции, цикла).
- Каждое изменение памяти → в журнал.
- Хранишь diff-структуру: переменная + старое значение.
- Вызов `rollback:` откатывает все diff’ы до ближайшего snap-point.

```c
snapshot: loop
	// изменения идут в лог
	while: x < 10 {
		x += 1;
	}
rollback: loop  // откат всех действий цикла
```
### Минусы/риски:
- overhead по памяти (можно оптимизировать: только изменяемое).    
- сложность с указателями, если не сделать их иммутабельными или привязанными к хип-объектам.    
- сайд-эффекты (IO, file) надо обернуть в "транзакционные обертки" — как `mocked`.    

* **Вердикт:** Это реально, даже на C. И звучит как убийственная killer-feature для инструмента автоматизации/отладки/обучения.
---
## **2. Execution Timelines (fork/merge)**
### Суть:
Механизм _ветвления выполнения_. Как `git branch`, но для control flow. Позволяет:
- тестировать сценарии без побочного эффекта,
- сравнивать поведение,
- "приклеивать" удачные ветки.
### Плюсы:
- **"Что если"-сценарии**: идеально для симуляторов, игр, стратегий, решений с вероятностями.
- **Многошаговая отладка**: можешь смотреть, как программа вела бы себя по-другому.
- **Код становится интерактивным**.[]()

```c
fork: timeline "fail_case" {
	x = 0;
	do_fail();
}

fork: timeline "success_case" {
	x = 1;
	do_work();
}

merge: timeline "success_case";
```
### Как реализовать:
- При `fork` создаешь **копию текущего состояния** (переменные + хип).    
- Все изменения идут в "изолированный слой".    
- `merge` перезаписывает главное состояние тем, что было в таймлайне.    
- Можно добавить `discard:` чтобы уничтожить неудачную ветку.    
### Минусы/сложности:
- нужен собственный _memory management layer_ (в идеале – GC или snapshot-based heap).    
- IO и внешние вызовы тоже должны быть изолированы.    
- синхронизация между таймлайнами сложная, если параллельны.    

* **Вердикт:** Ультимативный инструмент для симуляций, игр, ML-тренировок, "экспертных систем" и продвинутого дебага. Это будет фича, которой будут хвастаться.
---
## 3. Auto-tests (встроенные тесты)**
### Идея:
Писать тесты прямо в исходном коде.
```rust
test: add_two {
	assert: call: add_two(x = 1, y = 2) == 3;
}
```
### Плюсы:
- Улучшает читаемость.    
- Работает как документация.    
- Упрощает написание unit-тестов и CI-интеграцию.    
### Минусы:
- Нужен изолированный тестовый рантайм (можно решить).    
- Нужно ввести assert / expect / test-блоки в синтаксис (не сложно).    

**Вердикт:** **Добавлять обязательно.**  
- Простой, мощный инструмент. Отлично вписывается в концепцию.  
- Можешь даже сделать `test-mode:` для запуска всех `test:` из файла.
---
## 4. **State Solver** (SMT/Prolog-подход)
### Идея:
Позволить задать условия на результат, а интерпретатор сам вычислит входные значения.
```rust
solve: int8 x, y {
	where: x + y == 10, x > 0, y > 0;
}
```
### Зачем:
- **Генерация тестов**: найти краевые случаи и инпуты, вызывающие баги.    
- **Интерактивные задачи**: дать систему уравнений — она решит.    
- **Оптимизация**: можно использовать как декларативный способ параметризации.
### Плюсы:
- Близко к идеологии “описал _что_, а не _как_”.   
- Может быть отличительной чертой языка.    
- Хорошо сочетается с rollback/timeline — можно "перебирать" миры, пока условие не выполнится.
### Минусы:
- Реализация потребует простого SMT-решателя или биндинга к внешнему (Z3).
- Неуместно везде — в реальном коде часто важна не декларация цели, а контроль над алгоритмом.    
- Может "испугать" обычных пользователей.    

**Вердикт:** **Да, но с осторожностью.**  
- Реализуй сначала простую версию (`solve:` с логикой над целыми числами и логикой), подключи как модуль, а не встроенный синтаксис. Это будет киллер-фича для _автотестов_, _анализа_, _обратного проектирования_.
---
## 5. **Time Travel Debugging + Hot Reloading**
### Идея:
Прямо во время выполнения:
- откатываемся назад,    
- меняем код,    
- продолжаем как будто так и было.  
### Плюсы:
- **Мечта отладчика**. Особенно для сложных симуляций, игр, ML-экспериментов.    
- Позволяет "отрастить новый путь" без перезапуска.    
- В сочетании с `timeline:` превращает твой язык в отладочную машину времени.    
### Минусы:
- Очень сложно на уровне реализации (особенно: _Hot Reload_ кода функций + восстановление контекста).    
- Не сочетается с внешними вызовами без sandbox-а.    
- Нужно проектировать систему хранения стека/heap/контекста исполняемых функций.    
- На C это _тяжело_ (придётся эмулировать VM с полным контролем над callstack).    

**Вердикт:**  Идея мощная, но реализация — high effort.
- Ты можешь _заложить инфраструктуру_ сейчас (undo, timeline, snapshot), а потом добавить _Time Travel + Hot Reload_ как продвинутую функцию IDE или инструмента отладки.
---
## **1. `trace:` — трассировка исполнения**
Позволяет отслеживать, **что происходит в solve/simulate/fork**, какие ветки выбираются, какие значения пробуются, где происходят откаты и почему.
```rust
trace: solve_case {
	solve: int8 x, y {
		where: x + y == 10, x > 0, y > 0;
	}
	io: stdout << "Result: x=" << x << ", y=" << y;
}
```
### 🔍 Что даст:
- Пошаговое логирование попыток, ограничений, неудач.    
- В будущем — GUI для визуализации.    
- Для AI-планирования — ключевой инструмент дебага. 
---
## **2. `explain:` — почему было выбрано то, что выбрано**
Ты вызываешь `explain:` после `solve:` или `choose:`, и он показывает:
- какие альтернативы рассматривались,    
- почему они не подошли (ссылаясь на `where:`),    
- почему выбрано текущее решение.
```rust
solve: int8 x {
	where: x * 2 == 20, x < 15;
}
explain: x;
```

Вывод:
```
Tried: x = 0..14
Rejected:
  x = 0 → constraint failed: x * 2 == 20
  x = 1 → constraint failed: x * 2 == 20
  ...
Accepted: x = 10
```
---
## Как можно использовать в практике:
### Вместе с `simulate:`:
```rust
simulate: scenarios > [
	timeline "A" => { x = 10 },
	timeline "B" => { x = 20 },
	timeline "C" => {
		solve: int8 x where: x < 30;
	}
];
choose: timeline "C" where: x == 12;
explain: timeline;  // Почему именно "C"
```
### В тестах:
```rust
test: edge_case {
	trace: solve: int8 x {
		where: x * 3 == 99;
	}
	assert: x == 33;
	explain: x;
}
```
---
## Бонус — `trace level:` и `explain mode:`
Добавь уровни подробности:
```bc
trace: level debug;
explain: mode verbose;
```
---
## Реализация:
- `trace:` — просто логирование шагов в интерпретаторе (в solve/fork/simulate).
- `explain:` — хранение причин отказа (constraint-fail), сопоставление с веткой, возврат как дерево решений.
---
## **Нужен умный GC**
- **много высокоуровневых конструкций**: `solve:`, `fork:`, `simulate:` → это порождает временные объекты, альтернативные миры, состояния, которые сложно отслеживать вручную.
- При использовании **структур, списков, хэш-таблиц, timeline и rollback** — ручное управление памятью быстро станет кошмаром.
---
### Характеристики:
1. **Трассировка поколений + reference counting** — базовая и простая реализация.    
2. **Region-based GC / arena allocation** — идеально для `fork:` и `rollback:`:    
    - каждый `timeline` → свой memory region,        
    - при `merge:` регион поглощается,        
    - при `rollback:` — удаляется.        
    - Быстро, предсказуемо, без пауз.
---
```rust
// Типы данных
i8,  i16,  i32,  i64; // Знаковые
u8, u16, u32, u64;    // Беззнаковые
f32, f64              // Плавающая точка
void, bool, str;

// Структуры данных
list, hash_table, queue, stack, vector, tuple,

// Префиксы
let, io, mem, set, cast, struct, if, else if, else, match, case, default, while, for, fn, call, import, try, catch, defer, test, assert, fail, solve, where, watch, optimize, maximize, minimize, snapshot, rollback, undo, fork, compare, merge, discard, simulate , choose, run, trace, explain, type, extern, watch

// Создание переменных
let: i8 var_1;
let: i16 int_var_1 = 5, int_var_2 = 5;
let: str name = "Bread";
let: ptr<i16> ptr_var = &int_var_1;
let: list<i64> var_1;

// Ввод-вывод
io: stdout << "Hello, World!";
io: stdin >> input_buffer;
io: "file.txt" >> file_buffer;
io: "out.txt" << "some data";

// Проверка условий
if: int_var_1 == 5 {
	io: stdout << "Equals 5";
}

// Вложенные проверки условий
if: int_var_1 > 0 {
	if: int_var_1 < 10 {
		io: stdout << "Between 1 and 9";
	}
}

// match statement
io: stdin >> str input;
match: input {
	case: "yes"
		call: accept(); // вызов функций
		break;
	case: "no"
		call: decline();
		break;
	case: "i dont know"
		call: think();
		break;
	default:
		call: do_smthg();
		break;		
}

// Цикл while
while: int_var_1 < 10 {
	set: int_var_1 += 1;
}

// Цикл for
for: i64 i = 0; i < 10; i++ {
	if: i == is_even() {
		set: int_var_1 += 1;
	}
	else: {
		set: int_var_1 += 2;
	}
}

// Цикл for
for: i64 i in 0, 10 {
	set: int_var_1++; // через сет можно делать присваивание
}

// Создание функций
fn: sum(i16 x, i16 y) -> i16 {
	let: i16 result;
	set: result = x + y;
	return: result;
}

// Систему эффектов или `@effect` аннотации
// Может быть полезно в `solve:` / `simulate:`
fn: log_change(int x) @effect(io) {
	io: stdout << "x changed";
}

// Вызов функций
let: i64 result = add_two(x: 1, y: 3);

// Работа с модулями
module: math_utils::add_two;
let: i16 result = add_two(x: 1, y: 3);

// Проверка указателей
if: valid(ptr_var) {
	mem: write ptr_var = 42;
}

// Арифметические выражения
set: x = 3 * (y + 1);

// Приведения типов
cast: float_val >> i32 => int_val;

// Обработка ошибок
try: {
	io: "config.txt" >> cfg;
}
catch: {
	io: stderr << "File error";
}

// струтуры из си
struct: point {
	let: f64 x;
	let: f64 y;
}

// Tесты прямо в исходном коде
test: add_two {
	assert: call: add_two(x: 1, y: 2) == 3;
}

// Позволить задать условия на результат, а интерпретатор сам вычислит входные значения
solve: int8 x, y {
	where: x + y == 10, x > 0, y > 0;
}

// Декларативно описать "что должно быть", затем перебрать несколько возможных миров и выбрать нужный.
fork: timeline "guessing" {
	solve: i8 x, y {
		where: x + y == 10, x > 2, y > 2;
	}
	io: stdout << "Trying x=" << x << ", y=" << y;
}
//все эти идеи можно комбинировать между собой и это ядерная бомба

// Проверяем, подходит ли найденное значение
if: x == 6 {
	merge: timeline "guessing";
}
// Можно комбинировать `solve:` с `fork:` и `merge:`, чтобы найти нужную конфигурацию параметров

// Проверка гипотез, откат при неудаче, встроенные тесты
test: edge_case {
	fork: timeline "t1" =>
		set: x = 999;
		if: x >= 1000 {
			fail: "X went over limit";
		}
	rollback: timeline "t1";
}
// В тесте ты создаешь ветку, пробуешь "сломать" код, и если всё ок — идешь дальше
// Если нет — тест падает. `revert:` удаляет timeline, не внося изменений

// Автоматическая генерация входов, удовлетворяющих условию → прогон → валидация.
test: solve_example {
	solve: i8 x {
		where: x * 2 == 20, x < 15;
	}
	assert: x == 10;
}
// Можно использовать `solve:` как генератор тест-кейсов, а `assert:` — как валидацию результатов.

// Это будет "ветвление + выборка", с возможностью автоматического отбора наилучшего мира.
simulate: scenarios > [
	timeline "A" => { x = 10 },
	timeline "B" => { x = 20 },
	timeline "C" => { solve: x where: x < 30 }
];
choose: timeline "C" where: x == 12;

// Снимки состояния переменных
// Не путать с `snapshot:` исполнения. Здесь — конкретно значения переменных
let: snapshot state1 = snapshot_of(x, y, z);
rollback: state1;

// Поддержка внешних библиотек / FFI
extern: "libm.so" fn: sin(f64) -> f64;

// Пользовательские типы / алгебраические типы
type: Option<T> = Some(T) | None;

// Трассировка переменных / Watch
// Чтобы в `simulate:` или `solve:` видеть траекторию изменений
watch: x, y;

// Отложенные вычисления как в Go
defer: io: stdout << "Cleaning up...";

// Позволяет отслеживать, что происходит в solve/simulate/fork, какие ветки выбираются, какие значения пробуются, где происходят откаты и почему
trace: solve_case {
	solve: i8 x, y {
		where: x + y == 10, x > 0, y > 0;
	}
	io: stdout << "Result: x=" << x << ", y=" << y;
}

// Почему было выбрано то, что выбрано
solve: i8 x {
	where: x * 2 == 20, x < 15;
}
explain: x;

// Возможность добавить уровни подробности
trace: level debug;
explain: mode verbose;
```