специальный документ, который будет 

```
// variables
$datatype $name = $value; // C style
$name: $datatype = $value; // Rust style
$name = $value; // Python style

// functions
$datatype $name($args) // C style
{
	
}
fn $name($args) -> $datatype{
	
}
def $name:
	
```