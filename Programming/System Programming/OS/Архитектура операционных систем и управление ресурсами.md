#### **1. Основные требования к ОС**  
- **Для пользователя**:  
  - Простота освоения, удобство, надежность, безопасность, высокая скорость работы, минимальные требования к железу.  
- **Для разработчика**:  
  - Легкость проектирования, реализации и поддержки.  
  - Гибкость для обновлений и масштабирования.  

**Ключевая идея**:  
> Нет универсального алгоритма создания ОС — поэтому существуют разные системы (Windows, Linux, macOS), но все они решают общие задачи управления ресурсами.  

---  
#### **2. ОС как менеджер ресурсов**  
ОС координирует:  
- **Аппаратные ресурсы**: CPU, память, диски, устройства ввода-вывода.  
- **Абстракции для программ**:  
  - **Процессы** → Управление CPU.  
  - **Виртуальная память** → Управление физической памятью.  
  - **Файловая система** → Управление данными на диске.  
  - **Сетевые сервисы** → Коммуникация между устройствами.  

---  
#### **3. Основные сервисы ОС**  
1. **Планирование процессов**:  
   - Распределение времени CPU между задачами (например, браузер, музыка, код).  
2. **Управление памятью**:  
   - Выделение памяти процессам, изоляция, виртуальная память (своппинг на диск).  
   - Пример: 32-битная Windows дает каждому процессу 4 ГБ (2 ГБ ядро, 2 ГБ пользователь).  
3. **Управление устройствами (I/O)**:  
   - Драйверы скрывают сложность железа (например, запись в файл вместо управления диском).  
4. **Файловая система**:  
   - Иерархия файлов/папок — самый популярный способ организации данных.  
5. **Вспомогательные утилиты**:  
   - Форматирование дисков, проверка ошибок, поиск файлов (например, `chkdsk` в Windows).  
6. **Пользовательский интерфейс**:  
   - Текстовый (CLI) или графический (GUI). Серверы часто работают без GUI (headless-режим).  
7. **Системные вызовы**:  
   - API для безопасного взаимодействия программ с ядром (например, `open()` для файлов в Linux).  
8. **Защита ресурсов**:  
   - Разграничение прав (режимы *kernel/user*), предотвращение коллизий (например, Shared Memory в UNIX).  

---  
#### **4. Фундаментальные принципы современных ОС**  
- **Многозадачность**:  
  - Иллюзия параллельной работы процессов (например, музыка + браузер + код).  
- **Модульность (менеджеры)**:  
  - Каждый компонент отвечает за свою задачу:  
    - **Менеджер процессов** → Создание/завершение задач.  
    - **Планировщик** → Распределение времени CPU.  
    - **Менеджер памяти** → Виртуальная память, своппинг.  
    - **Менеджер безопасности** → Контроль доступа (например, `sudo` в Linux).  
- **Микроядро vs Монолитное ядро**:  
  - *Микроядро* (например, MINIX) — только базовые функции, остальное в пользовательском пространстве.  
  - *Монолитное* (Linux, Windows) — всё в ядре (выше производительность, но сложнее维护).  

---  
#### **5. Управление процессами**  
- **Процесс** = Программа в execution:  
  - Состоит из: потоков (threads), состояния (контекста), ресурсов (память, файлы).  
- **Потоки (threads)**:  
  - Легковесные "нити" внутри процесса (делят память, но имеют свой счетчик команд).  
  - Пример: браузер Chrome — 1 процесс, но много потоков (вкладки, рендеринг).  
- **Планирование**:  
  - **Долгосрочное**: Какие процессы запускать (например, при старте системы).  
  - **Среднесрочное**: Своппинг (выгрузка процессов на диск).  
  - **Краткосрочное**: Выбор следующего процесса для CPU (алгоритмы: Round Robin, FIFO).  

---  
#### **6. Управление памятью**  
- **Виртуальная память**:  
  - Каждый процесс "видит" свою память (4 ГБ в 32-битной системе), хотя физическая память меньше.  
  - **Страничная организация**: Память делится на страницы (обычно 4 КБ), которые могут быть в RAM или на диске.  
- **Кэширование**:  
  - Иерархия: Диск → RAM → Кэш CPU → Регистры.  
  - Алгоритмы замены (например, LRU — Least Recently Used).  

---  
#### **7. Ввод-вывод (I/O)**  
- **Синхронный I/O**:  
  - Программа ждет завершения операции (например, печать документа).  
- **Асинхронный I/O**:  
  - Программа продолжает работу (например, загрузка файла в фоне).  
- **Драйверы**:  
  - Преобразуют запросы ОС в команды для конкретного устройства (например, принтера).  

---  
### **Заключение**  
ОС — это сложный "дирижер", который:  
- Управляет ресурсами (CPU, память, I/O).  
- Изолирует процессы для безопасности.  
- Дает удобные абстракции (файлы, сокеты).  

**Что дальше?**  
- Детальный разбор алгоритмов планирования (Round Robin, многоуровневые очереди).  
- Глубокое погружение в виртуальную память и кэширование.  
- Примеры системных вызовов (как программа общается с ядром).  

---  
🔧 **Практика**:  
1. Запустите `top` (Linux) или Диспетчер задач (Windows) — посмотрите, как ОС распределяет ресурсы.  
2. Напишите простой многопоточный код на Python/C — почувствуйте, как потоки делят CPU.  
3. Изучите `mmap` (Linux) — механизм отображения файлов в память.

[[Operation system]]