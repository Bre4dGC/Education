Хранение денежного эквивалента в целочисленных переменных (например, int или long) - это распространённый и эффективный подход для работы с деньгами в программировании. Использование целых чисел для хранения денежных сумм помогает избежать проблем, связанных с точностью вычислений с плавающей запятой.

🟠Точность
Вычисления с плавающей запятой могут приводить к погрешностям из-за ограниченной точности представления чисел, что недопустимо при работе с денежными суммами.

🟠Производительность
Операции с целыми числами обычно выполняются быстрее, чем с числами с плавающей запятой.

🟠Безопасность
Целочисленные значения исключают ошибки округления и обеспечивают предсказуемость результатов вычислений.

🚩Как это используется?

Для хранения денежных сумм в целочисленных переменных обычно выбирается самая маленькая денежная единица (например, копейки или центы) и используется вместо основной единицы (рубли или доллары). Таким образом, все суммы хранятся в виде целого числа, представляющего количество самых маленьких денежных единиц.

🚩Пример
```C++
class Money {
public:
    // Конструктор принимает сумму в долларах и центах
    Money(int dollars, int cents) {
        totalCents = dollars * 100 + cents;
    }

    // Получить общую сумму в центах
    int getTotalCents() const {
        return totalCents;
    }

    // Операция сложения денежных сумм
    Money operator+(const Money& other) const {
        return Money(0, totalCents + other.totalCents);
    }

    // Вывод суммы в долларах и центах
    void print() const {
        std::cout << "$" << totalCents / 100 << "." << totalCents % 100 << std::endl;
    }

private:
    int totalCents;
};

int main() {
    Money m1(5, 75); // $5.75
    Money m2(3, 50); // $3.50

    Money m3 = m1 + m2; // Сумма $5.75 и $3.50

    m1.print(); // Вывод: $5.75
    m2.print(); // Вывод: $3.50
    m3.print(); // Вывод: $9.25

    return 0;
}
```
[[C++]]