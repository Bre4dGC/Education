![[{20651503-2E8D-46E8-91EC-1CFFDA84979B}.png|500]]
###### В C можно не только передавать данные в функции, но и сами функции! 
Указатель на функцию — это переменная, которая хранит адрес функции. Вместо вызова функции напрямую, можно обратиться к ней через указатель.
#### ☑ Объявление указателя на функцию:
```C
int (*ptr)(int, int); // ptr — указатель на функцию, принимающую 2 int и возвращающую int
```
#### ☑ Присваивание адреса функции:
```C
int sum(int a, int b) { return a + b; }
ptr = sum;  // Теперь ptr указывает на функцию sum
```
#### ☑ Вызов через указатель:
```C
int result = ptr(5, 3);  // Работает так же, как sum(5, 3)
```
Указатели на функции дают мощный инструмент для динамического управления программой 🔥
[[C]]