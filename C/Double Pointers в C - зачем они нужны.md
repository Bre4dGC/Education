![[Pasted image 20250403142449.png]]
Если вы работаете с динамической памятью, связанными списками или деревьями, то double pointers — ваш бро. Они дают мощный контроль над памятью и позволяют гибко управлять данными.
#### Где используются двойные указатели?
* Связанные списки — позволяют изменять `head` внутри функций (без них было бы сложнее!).
* Деревья и графы — удобны для вставки, удаления узлов и балансировки.
* Динамическое выделение памяти — передача указателя по ссылке предотвращает утечку памяти.
##### Как это работает? 🤔
Двойной указатель (`**ptr`) — это указатель на указатель. Он позволяет изменять сам указатель, а не только его содержимое. Пример:
```C
#include <stdio.h>
#include <stdlib.h>

void allocateMemory(int **ptr) {
    *ptr = (int *)malloc(sizeof(int)); 
    **ptr = 42;
}

int main() {
    int *num = NULL;
    allocateMemory(&num);
    printf("%d\n", *num); // Выведет 42
    free(num);
    return 0;
}
```
Здесь `allocateMemory()` изменяет `num внутри main()`, потому что мы передаем адрес указателя.
[[C]]